# Requirements for running Phaser demos on Mac
# 
# Preferred method (using uv):
#   uv venv --python 3.12
#   source .venv/bin/activate
#   uv sync
#
# Alternative (using pip):
#   pip install -r requirements.txt

pyadi-iio>=0.0.15
pyqtgraph>=0.13.0
PyQt5>=5.15.0
numpy>=1.20.0
scipy>=1.7.0
